
// todo: handle immutability
struct MinMax {
  min: u32,
  max: u32,
}

fn main() {
  let m1 = MinMax { min: 3, max: 7 };
  mut m2 = MinMax { min: 1789, max: 1998 };

  m2.max = 2021;
  
  (MinMax {
    min: m1.min + m2.min,
    max: m1.max + m2.max 
  }).max
}