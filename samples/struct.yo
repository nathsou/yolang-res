
struct MinMax {
  min: u32,
  max: u32
}

struct MinMax2 {
  min: u32,
  maxx: u32
}

fn reset(m) {
  m.min = 0;
  m.max = 0;
}

fn main() {
  let m = MinMax {
    min: 1789,
    max: 1998
  };

  reset(m);

  m.min + m.max == 0
}