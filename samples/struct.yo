
struct MinMax {
  min: u32,
  max: u32
}

struct Test {
  a: u32,
  b: MinMax,
  c: MinMax,
}

fn main() {
  let t = Test {
    a: 3,
    b: MinMax { min: 10, max: 100 },
    c: MinMax { min: 1789, max: 1998 },
  };

  t.c.min
}