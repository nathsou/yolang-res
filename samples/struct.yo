
// todo: handle immutability
struct MinMax {
  min: u32,
  max: u32,
}

fn main() {
  mut m = MinMax {
    min: 1789,
    max: 1998
  };

  let diff = (a, b) -> a - b;

  m.max = 2021;
  
  diff(m.max, m.min)
}