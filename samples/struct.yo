
struct MyStruct {
  a: (),
  b: (),
  c: (),
  d: u32,
  e: (),
  f: bool
}

fn main() {
  let test = MyStruct {
    a: (),
    b: (),
    c: (),
    d: 17,
    e: (),
    f: true
  };

  let and = (a, b) -> if a { b } else { false };

  and(test.a == test.e, and(test.d == 17, test.f))
}