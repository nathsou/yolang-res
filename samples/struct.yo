
struct MinMax {
  min: u32,
  max: u32
}

impl MinMax {
  fn reset(self) {
    self.min = 0;
    self.max = 0;
  }
}

fn main() {
  let m = MinMax {
    min: 1789,
    max: 1998
  };

  m.reset();

  m.min + m.max == 0
}