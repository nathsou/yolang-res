
struct Str {
  start: Ptr<char>,
  len: u32,
}

extern fn printString(start: Ptr<char>, len: u32) -> ();

fn print(slice) {
  printString(slice.start, slice.len);
}

fn main() {
  let hello = Str {
    start: ['h', 'e', 'l', 'l', 'o'] as Ptr<char>,
    len: 5,
  };

  print(hello);
}